[package]
name = "SteamUploader"
authors = ["SimKDT"]
description = "A simple tool to upload content to the Steam Workshop using Rust and the steamworks-rs library."
license = "Custom (see LICENSE file)"
readme = "README.md"
repository = "https://github.com/SirDoggyJvla/SteamUploader-rs"
version = "0.3.0"
keywords = ["steam", "workshop", "uploader", "rust"]
categories = ["games"]
edition = "2021"
rust-version = "1.80.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# args parsing
clap = { version = "4", features = ["derive"] }

# interactive CLI menu
inquire = "0.7"

# Steamworks SDK bindings
steamworks = { version = "0.13.0", features = ["raw-bindings"] }

# for manifest parsing and handling
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
serde_yaml = "0.9"

termcolor = "1.4"
confy = "2.0.0"
